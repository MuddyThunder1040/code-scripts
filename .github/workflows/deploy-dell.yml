name: Deploy Dell Services

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'dell/services/portainer/**'

jobs:
  deploy:
    runs-on: [self-hosted, Linux, X64]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.HOMELAB_DEPLOY_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan 100.64.213.62 >> ~/.ssh/known_hosts

      - name: Rsync portainer compose to /data/services/portainer
        run: |
          mkdir -p /data/services/portainer
          rsync -az dell/services/portainer/ /data/services/portainer/

      - name: Deploy portainer
        run: docker compose -f /data/services/portainer/compose.yml up -d

      - name: Verify containers are healthy
        run: |
          sleep 15
          unhealthy=$(docker ps --filter "health=unhealthy" --format "{{.Names}}")
          if [ -n "${unhealthy}" ]; then
            echo "Unhealthy containers: ${unhealthy}"
            exit 1
          fi
          echo "All containers healthy"

      - name: Notify Slack (success)
        if: success()
        run: |
          curl -s -X POST -H 'Content-type: application/json' \
            --data '{
              "attachments": [{
                "color": "good",
                "text": "Dell services deployed successfully from branch ${{ github.ref_name }} (commit ${{ github.sha }})."
              }]
            }' \
            "${{ secrets.SLACK_WEBHOOK_URL }}"

      - name: Notify Slack (failure)
        if: failure()
        run: |
          curl -s -X POST -H 'Content-type: application/json' \
            --data '{
              "attachments": [{
                "color": "danger",
                "text": "Dell services deployment failed on branch ${{ github.ref_name }} (commit ${{ github.sha }})."
              }]
            }' \
            "${{ secrets.SLACK_WEBHOOK_URL }}"
